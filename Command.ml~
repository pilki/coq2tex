open Sig
open Printf

module Cmd (Out:OUTCHAN) = struct
  open Out

  let output s = output_string out s

  let space n =
    for i = 1 to n do
      output Conf.space_cmd
    done

  let newline () =
    output Conf.newline_cmd

  let keyword s =
    
